# 报销系统项目架构说明 - 面向 Java 开发者

> 本文档专门为有 Java 后端开发经验的开发者准备，会将前端概念与 Java 进行类比。

---

## 📁 项目结构总览

```
reimbursement/                    # 根目录（类似 Maven 多模块项目）
├── apps/                         # 应用模块目录
│   ├── api/                      # 后端 API（类似 Spring Boot）
│   ├── web/                      # 前端 Web（类似 JSP/Thymeleaf，但更现代）
│   └── worker/                   # 后台任务处理器（类似 Spring Batch/Quartz）
├── packages/                     # 共享代码包
│   └── shared/                   # 共享类型和工具（类似公共 common 包）
└── package.json                  # 依赖管理（类似 pom.xml）
```

---

## 🎯 技术栈对比（Java vs 本项目）

| Java 技术栈 | 本项目技术 | 说明 |
|------------|-----------|------|
| Spring Boot | Hono | Web 框架，类似 @RestController |
| JPA/Hibernate | Drizzle ORM | ORM 框架，类似 Entity Manager |
| Maven/Gradle | npm/pnpm | 包管理工具 |
| @Service | 函数式服务 | 服务层，但是函数而非类 |
| @Controller | Hono Routes | 路由处理，类似 Controller |
| PostgreSQL | PostgreSQL | 数据库（一样！） |
| POJO | TypeScript Interface | 数据对象定义 |
| Spring Security | JWT + 中间件 | 认证授权 |
| Scheduled Tasks | Worker | 定时任务 |
| JSP/Thymeleaf | React/Next.js | 前端视图 |

---

## 🏗️ 核心概念对比

### 1. **TypeScript vs Java**

```typescript
// TypeScript - 类型是可选的，编译时检查
interface User {              // 类似 Java interface，但只定义结构
  id: string;
  name: string;
  email?: string;            // ? 表示可选，类似 @Nullable
}

type Status = "pending" | "approved";  // 联合类型，类似枚举
```

```java
// Java - 强制类型
public class User {
    private String id;
    private String name;
    @Nullable
    private String email;
}

enum Status { PENDING, APPROVED }
```

### 2. **函数式编程 vs 面向对象**

```typescript
// TypeScript - 函数式（本项目主要风格）
export async function createUser(input: CreateUserInput): Promise<User> {
  return db.insert(users).values(input).returning();
}

// 类似 Java 的静态方法：
// public static User createUser(CreateUserInput input) { ... }
```

```java
// Java - 面向对象
@Service
public class UserService {
    public User createUser(CreateUserInput input) { ... }
}
```

### 3. **异步处理 vs 同步**

```typescript
// TypeScript - async/await（类似 CompletableFuture）
async function fetchUser(id: string): Promise<User> {
  const user = await db.query.users.findFirst({ where: eq(users.id, id) });
  return user;
}

// 调用
const user = await fetchUser("123");  // 等待结果
```

```java
// Java - CompletableFuture
CompletableFuture<User> fetchUser(String id) {
    return CompletableFuture.supplyAsync(() ->
        userRepository.findById(id).orElseThrow()
    );
}

// 调用
User user = fetchUser("123").get();  // 阻塞等待
```

---

## 📦 各应用详解

### 🔷 apps/api - 后端 API

**类比：Spring Boot 应用**

```
apps/api/
├── src/
│   ├── index.ts           # 应用入口（类似 @SpringBootApplication）
│   ├── server.ts          # 服务器启动（类似 main 方法）
│   ├── config.ts          # 配置文件（类似 application.yml）
│   ├── routes/            # 路由层（类似 @RestController）
│   │   ├── auth.ts        # 认证相关接口
│   │   ├── projects.ts    # 项目管理接口
│   │   └── receipts.ts    # 回执管理接口
│   ├── services/          # 服务层（类似 @Service）
│   │   ├── auth.ts        # 认证服务
│   │   └── storage.ts     # 文件存储服务
│   ├── middleware/        # 中间件（类似 Filter/Interceptor）
│   │   └── auth.ts        # JWT 认证中间件
│   └── db/                # 数据库层（类似 Repository）
│       ├── schema.ts      # 表结构定义（类似 @Entity）
│       └── client.ts      # 数据库连接（类似 DataSource）
└── package.json           # 依赖配置（类似 pom.xml）
```

**核心文件作用：**

- **index.ts** - 定义所有路由和中间件（类似配置所有的 @RequestMapping）
- **routes/*.ts** - 各个 API 端点（每个文件类似一个 @RestController）
- **services/*.ts** - 业务逻辑（类似 @Service 类）
- **middleware/auth.ts** - JWT 验证（类似 Spring Security Filter）

### 🔷 apps/web - 前端应用

**类比：传统 JSP + jQuery，但是现代化版本**

```
apps/web/
├── app/                    # 页面目录（每个文件夹 = 一个页面路由）
│   ├── layout.tsx          # 全局布局（类似 JSP 模板）
│   ├── page.tsx            # 首页（类似 index.jsp）
│   ├── login/              # /login 路径
│   │   └── page.tsx        # 登录页
│   └── projects/           # /projects 路径
│       └── page.tsx        # 项目列表页
├── components/             # 可复用组件（类似自定义标签库）
│   ├── Button.tsx          # 按钮组件
│   └── ProjectCard.tsx     # 项目卡片组件
├── hooks/                  # React Hooks（类似工具类，但有状态）
│   └── useAuth.ts          # 认证相关逻辑
└── lib/                    # 工具库
    ├── api.ts              # API 调用（类似 RestTemplate）
    └── db.ts               # 本地数据库（IndexedDB）
```

**重要概念：**

1. **组件（Component）** = Java 中的可复用 UI 类
   ```tsx
   // React 组件（类似自定义 JSP 标签）
   function Button({ text, onClick }) {
     return <button onClick={onClick}>{text}</button>;
   }
   ```

2. **Hooks** = 带状态的工具函数
   ```typescript
   // useState 类似类的成员变量
   const [count, setCount] = useState(0);  // 类似: private int count = 0;
   ```

3. **路由** = 基于文件系统
   ```
   app/projects/page.tsx  →  访问 /projects
   app/login/page.tsx     →  访问 /login
   ```

### 🔷 apps/worker - 后台任务

**类比：Spring Batch / Quartz 定时任务**

```
apps/worker/
├── src/
│   ├── index.ts           # 任务调度器（类似 @Scheduled）
│   ├── jobs/              # 具体任务（类似 Job 实现）
│   │   └── export.ts      # 导出任务
│   └── services/          # 任务使用的服务
│       └── storage.ts     # 文件存储
```

### 🔷 packages/shared - 共享代码

**类比：Maven 公共模块（common）**

```
packages/shared/
├── src/
│   ├── db/
│   │   └── schema.ts      # 数据库表定义（所有应用共享）
│   ├── domain/
│   │   └── types.ts       # 业务类型定义（类似 DTO）
│   └── utils/             # 工具函数（类似 Utils 类）
```

---

## 🔄 数据流向（从前端到后端）

### 场景：用户创建项目

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. 用户点击"创建项目"按钮                                           │
│    apps/web/app/projects/page.tsx                               │
└─────────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. 调用 API 客户端                                                │
│    apps/web/lib/api.ts                                          │
│    fetch("/api/v1/projects", { method: "POST", ... })          │
└─────────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. 请求到达后端路由                                                │
│    apps/api/src/routes/projects.ts                             │
│    类似: @PostMapping("/api/v1/projects")                       │
└─────────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│ 4. 中间件验证 JWT                                                  │
│    apps/api/src/middleware/auth.ts                             │
│    类似: @PreAuthorize("hasRole('USER')")                       │
└─────────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│ 5. 路由处理器调用数据库                                             │
│    使用 Drizzle ORM 插入数据                                       │
│    类似: projectRepository.save(project)                        │
└─────────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│ 6. 返回 JSON 响应                                                  │
│    { id: "123", name: "项目A", ... }                             │
└─────────────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│ 7. 前端接收响应，更新 UI                                            │
│    类似 jQuery 的 AJAX 成功回调                                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔑 关键概念速查

### ES Modules (import/export)

```typescript
// 导出（类似 public class）
export function hello() { ... }
export const name = "张三";

// 导入（类似 import 语句）
import { hello, name } from "./utils";

// 默认导出（类似主类）
export default class User { ... }
import User from "./User";  // 可以任意命名
```

### Promise 和 async/await

```typescript
// Promise 类似 CompletableFuture
const promise = fetchData();  // 返回 Promise<Data>
promise.then(data => console.log(data));

// async/await 语法糖
async function main() {
  const data = await fetchData();  // 等待异步操作完成
  console.log(data);
}
```

### 解构赋值

```typescript
// 对象解构（类似提取字段）
const user = { id: "1", name: "张三", age: 25 };
const { id, name } = user;  // 等同于：const id = user.id;

// 数组解构
const [first, second] = [1, 2, 3];  // first=1, second=2
```

### 箭头函数

```typescript
// 传统函数
function add(a, b) { return a + b; }

// 箭头函数（简写）
const add = (a, b) => a + b;

// 类似 Java Lambda
// (a, b) -> a + b
```

---

## 📚 学习路线建议

作为 Java 开发者，推荐按以下顺序阅读代码：

### 第一阶段：后端（熟悉的领域）
1. ✅ `apps/api/src/config.ts` - 配置文件
2. ✅ `apps/api/src/db/client.ts` - 数据库连接
3. ✅ `packages/shared/src/db/schema.ts` - 表结构定义
4. ✅ `apps/api/src/routes/auth.ts` - 认证接口
5. ✅ `apps/api/src/services/auth.ts` - 认证服务

### 第二阶段：前端基础
6. ⏳ `apps/web/lib/api.ts` - API 调用封装
7. ⏳ `apps/web/app/layout.tsx` - 全局布局
8. ⏳ `apps/web/app/login/page.tsx` - 登录页面
9. ⏳ `apps/web/components/Button.tsx` - 简单组件

### 第三阶段：前端进阶
10. ⏳ `apps/web/hooks/useAuth.ts` - React Hooks
11. ⏳ `apps/web/app/projects/page.tsx` - 复杂页面
12. ⏳ `apps/web/lib/db.ts` - IndexedDB 本地存储

---

## 🆘 常见疑惑解答

### Q1: 为什么没有类（class）？
**A:** JavaScript/TypeScript 支持函数式编程。很多代码用纯函数代替类方法：
```typescript
// 函数式（本项目风格）
export function createUser(data: UserInput) { ... }

// 面向对象（也可以，但不常用）
export class UserService {
  createUser(data: UserInput) { ... }
}
```

### Q2: async/await 是什么？
**A:** 异步编程语法，类似 Java 的 CompletableFuture：
```typescript
// 等待数据库查询完成
const user = await db.query.users.findFirst();

// 类比 Java
User user = userRepository.findById(id).get();
```

### Q3: ? 和 ! 是什么意思？
**A:** TypeScript 的类型标记：
```typescript
email?: string;    // 可选属性，类似 @Nullable
email!: string;    // 断言非空，类似 @NotNull（强制）
```

### Q4: 前端的"状态"是什么？
**A:** 类似对象的成员变量，但会触发 UI 重新渲染：
```typescript
const [count, setCount] = useState(0);  // 类似: private int count = 0;
setCount(count + 1);  // UI 会自动更新显示新的 count
```

---

## 📖 下一步

我会为核心文件添加详细的行内注释，按以下顺序：

1. ✅ 后端 API 核心文件（你最熟悉的部分）
2. ⏳ 数据库 Schema
3. ⏳ 前端 API 调用
4. ⏳ 前端页面组件

**需要我现在开始添加注释吗？**
